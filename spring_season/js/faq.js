function load(){let hash=location.hash,faqs=document.querySelectorAll("details");for(let i=0;i<faqs.length;i++){const el=faqs[i],id=`#${el.name}`;id==hash&&(el.scrollIntoView(),el.open=!0)}}function removeHash(){history.replaceState(null,document.title,window.location.pathname+window.location.search)}function mark(el){console.log(el),window.location.replace(`#${el.name}`)}load(),document.querySelectorAll("details").forEach(details=>{details.addEventListener("toggle",event=>{event.target===details&&(event.target.open?mark(details):removeHash())})})